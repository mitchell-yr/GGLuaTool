<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Android 10及以下版本的存储权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Android 11及以上版本的管理所有文件权限 -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Android 13及以上版本的媒体权限（如果需要） -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.悠然GG助手"
        tools:targetApi="31"
        android:requestLegacyExternalStorage="true">
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".licenseModel.OpenSourceActivity"
            android:label="开源许可"
            android:theme="@style/Theme.悠然GG助手"
            android:parentActivityName=".MainActivity">
            <!-- 如果支持API 16以下，添加以下meta-data -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".CodeEditorLua"
            android:label="lua脚本编辑"
            android:theme="@style/Theme.悠然GG助手"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:parentActivityName=".MainActivity">
            <!-- 如果支持API 16以下，添加以下meta-data -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".template.TemplateManagerActivity"
            android:label="模板管理"
            android:theme="@style/Theme.悠然GG助手"
            android:parentActivityName=".MainActivity">
            <!-- 如果支持API 16以下，添加以下meta-data -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".tutorial.EncryptTutorialActivity"
            android:theme="@style/Theme.悠然GG助手" />
        <activity
            android:name=".tutorial.GGFunctionDocumentViewActivity"
            android:theme="@style/Theme.悠然GG助手"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity
            android:name=".tutorial.GGTutorialActivity"
            android:theme="@style/Theme.悠然GG助手"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity
            android:name=".tutorial.GGFunctionAddingTutorialActivity"
            android:theme="@style/Theme.悠然GG助手"
            android:parentActivityName=".MainActivity">
        </activity>
        <!-- 插件管理Activity -->
        <activity
            android:name=".plugin.PluginManagerActivity"
            android:label="插件管理"
            android:theme="@style/Theme.Material3.DayNight.NoActionBar" />
        <!-- Markdown查看器Activity -->
        <activity
            android:name=".MarkdownViewerActivity"
            android:label="文档查看"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:parentActivityName=".plugin.PluginManagerActivity" />
        <activity
            android:name=".LuaExecutorActivity"
            android:label="Lua脚本执行器"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />
        <activity
            android:name=".template.TemplateEditorActivity"
            android:label="模板管理"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />
        <activity
            android:name=".VisualLuaScriptEditor.luaksh.MainActivity"
            android:label="lua可视化">
        </activity>
        <activity
            android:name=".apktools.ModifierActivity"
            android:label="GGapk生成器">
        </activity>
        <activity
            android:name=".apktools.FunctionManagerActivity"
            android:label="函数管理">
        </activity>
    </application>
    <queries>
        <package android:name="com.tc" />
    </queries>
</manifest>